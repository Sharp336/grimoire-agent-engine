# oh-my-pi Fork 维护规则

本仓库仅保留少量、模块化、边界清晰且易于在新基线上重新应用的 Fork 差异，并遵守以下规则：

## 上游基线与维护边界

- 仅同步上游正式发布的 tag，不跟随、合并或采用上游 `main` 及 tag 之后的开发提交。
- 不修改 Rust 代码、上游构建逻辑、构建脚本或官方 native packages；Rust 源码、native package 版本和源码版本必须与对应的上游正式发布基线一致，不自行编译、重打包或打补丁。
- 不新增 Fork 专用测试文件或测试用例；验证复用上游已有测试、现有检查和必要的人工验收。

## Fork 功能差异

- 模型选择界面仅显示 OpenCode Go、OpenCode Zen、OpenAI Codex、DeepSeek，以及所有非 bundled 的自定义 Provider；该限制仅影响界面可见性，不改变底层 Provider 的可用性。**其中 `opencode-zen` 进一步仅显示免费模型（`cost.input === 0 && cost.output === 0`），已通过 `modelRoles` 配置或当前会话选中的付费模型白名单保留在列表中，其余付费模型在 Model Hub / Model Picker 中隐藏；该过滤仅影响显示，不影响底层可用性与 `--model` 直连。**
- Hub 默认使用精简模式，并允许切换为与上游一致的完整模式；两种模式必须共用上游 Hub runtime，不复制或改写其核心业务实现。
- Plan 模式必须先还原用户真正需要解决的问题，自行确认可从对话或仓库探索中确定的信息；仅询问仍会改变最终行为、范围或方案的关键歧义，不得替用户作关键假设。

## 开发与验证流程

- 大型修改，包括功能开发、跨模块变更和上游同步，必须在 `dev` 分支完成，并通过面向 `main` 的 PR 触发 GitHub CI；全部 CI 通过后方可合并。
- 小型文档修改或其他低风险修改可以直接在 `main` 完成。
- 本地仅按修改风险运行必要的快速定向检查，不运行完整或耗时测试；完整 CI 仅在 GitHub 上运行。

## Fork Release

- Fork Release 仅允许从 `main` 手动触发。
- 发布必须基于对应的上游正式版本，不得使用上游尚未正式发布的代码。

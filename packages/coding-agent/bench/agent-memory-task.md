Read `packages/coding-agent/tsconfig.json` exactly once. Do not inspect any other file or use another research tool.

Yield one structured result with:
- `summary`: one sentence naming the file's `extends` and `include` values.
- `files`: one item whose `path` is `packages/coding-agent/tsconfig.json` and whose `description` states those values.
- `architecture`: one sentence explaining that the package inherits the workspace TypeScript configuration.

Repeated `{{family}}` failures matched a stored procedure. Read `{{target}}` before the next `{{family}}` attempt.

Advisory, not authoritative: it was written from an earlier session and may be stale or wrong here. Current repository state and tool output win. After reading, say in one line whether it applies, then proceed.

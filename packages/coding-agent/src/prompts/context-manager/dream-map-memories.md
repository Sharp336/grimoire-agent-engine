Map each supplied project memory to the smallest set of repository files that directly support it. Search the repository when needed. If the memory is a durable repository-wide convention not backed by a specific file, mark it file-independent. Output `{ "actions": [{ "id": string, "action": "map", "backingFiles": string[], "fileIndependent": boolean, "reason": string }] }`. Paths must be project-relative. Include every supplied ID exactly once.
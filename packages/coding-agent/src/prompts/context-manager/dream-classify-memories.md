Classify each supplied unknown/context project memory into one strict durable type: `PROJECT_RULES`, `ARCHITECTURE`, `CONSTRAINTS`, `CONFIG_VALUES`, `NAMING`, or `FACT`. Output `{ "actions": [{ "id": string, "memoryType": string, "reason": string }] }`. Use CONSTRAINTS only for externally imposed limits, ARCHITECTURE for load-bearing decisions and rationale, and FACT only when no narrower durable category applies.
Curate the supplied project memories. Merge only true semantic duplicates, invalidate contradicted or low-value transient entries, and preserve distinct durable facts. Output `{ "actions": [{ "action": "merge", "ids": string[], "reason": string } | { "action": "invalidate", "id": string, "reason": string }] }`. Every referenced ID must come from the input. Do not rewrite merely for style.
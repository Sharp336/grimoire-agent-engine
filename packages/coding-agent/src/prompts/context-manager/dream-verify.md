Verify only the supplied memories whose mapped files changed or disappeared. Read current files before deciding. Output `{ "actions": [{ "id": string, "action": "keep" | "update" | "invalidate", "content"?: string, "backingFiles"?: string[], "reason": string }] }`. Keep means the fact still holds; update requires a complete durable replacement; invalidate means it is false or obsolete. Never act on an ID absent from the input.
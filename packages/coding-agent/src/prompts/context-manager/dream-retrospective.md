Extract durable knowledge only from the supplied explicit user corrections and re-explanations. Output `{ "memories": [{ "scope": "project" | "user", "category": string, "content": string, "sourceIndexes": number[] }] }`. Project entries must describe durable repository facts. User entries must be stable preferences, instructions, personality, or relationship context. Drop one-off task instructions and transient state.
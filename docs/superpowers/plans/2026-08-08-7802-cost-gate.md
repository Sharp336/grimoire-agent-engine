# #7802 成本门禁(--warn-cost / --max-cost)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 omp 增加原生成本门禁:CLI `--warn-cost`/`--max-cost` + 配置 `session.warnCost`/`session.maxCost`,在每次 provider 分发前检查累计会话成本,达 warn 阈值一次性警告、达 max 阈值拒绝下一次分发并停止轮次。

**Architecture:** 新增 `session/cost-gate.ts` 模块(纯逻辑:控制器、决策函数、错误类型),`main.ts` 从 flag+设置构建控制器,`createAgentSession` 的 streamFn 包装器在每次分发前调用门禁(成本 getter 由根会话首次绑定),子代理会话经 `executor.ts` 透传同一控制器读同一累计成本。

**Tech Stack:** bun, TypeScript, bun:test, settings-schema, 镜像现有 `--max-time`/deadline 模式

**规格:** `docs/superpowers/specs/2026-08-08-7802-cost-gate-design.md`

## Global Constraints

- 本 worktree 的 `packages/natives/native/index.js` 是本地 stub(真 addon 未编译),测试直接跑;若已恢复真实文件而 `.node` 不存在,先重新执行 stub 替换
- 测试文件用动态 `import()`,保证 stub 环境可加载
- 配置优先级:CLI flag > session 配置键 > 无门禁
- 门禁只拒绝下一次分发,不打断在途请求;print 模式触顶 exit 0(镜像 --max-time)
- 每任务结束提交,提交信息遵循仓库风格(`feat(cli): ...` / `feat(session): ...`)

---

### Task 1: cost-gate.ts 纯逻辑模块 + 单元测试

**Files:**
- Create: `packages/coding-agent/src/session/cost-gate.ts`
- Test: `packages/coding-agent/test/cost-gate.test.ts`(新建)

**Interfaces:**
- Produces(后续任务消费):
  - `interface CostGateController { warnCost?: number; maxCost?: number; warned: boolean; getCost?: () => number }`
  - `class CostCapExceededError extends Error { constructor(public readonly cap: number) }`
  - `function createCostGateController(options: { warnCost?: number; maxCost?: number }): CostGateController`
  - `function evaluateCostGate(controller: CostGateController, cost: number): "ok" | "warn" | "cap"`
  - `function applyCostGate(gate: CostGateController, getCost: () => number, onWarn: (message: string) => void, dispatch: () => unknown): unknown`
  - `function resolveCostGate(flags: { warnCost?: number; maxCost?: number }, configured: { warnCost?: number; maxCost?: number }): CostGateController | undefined`

- [ ] **Step 1: 写失败测试**

```ts
// test/cost-gate.test.ts
import { describe, expect, it } from "bun:test";
import {
	CostCapExceededError,
	applyCostGate,
	createCostGateController,
	evaluateCostGate,
	resolveCostGate,
} from "@oh-my-pi/pi-coding-agent/session/cost-gate";

describe("evaluateCostGate", () => {
	it("is ok when no thresholds are set", () => {
		expect(evaluateCostGate(createCostGateController({}), 999)).toBe("ok");
	});

	it("is ok below both thresholds", () => {
		const gate = createCostGateController({ warnCost: 8, maxCost: 10 });
		expect(evaluateCostGate(gate, 5)).toBe("ok");
	});

	it("warns once at warnCost and stays ok afterwards", () => {
		const gate = createCostGateController({ warnCost: 8, maxCost: 10 });
		expect(evaluateCostGate(gate, 8)).toBe("warn");
		expect(evaluateCostGate(gate, 9)).toBe("ok");
	});

	it("caps at maxCost regardless of prior warning", () => {
		const gate = createCostGateController({ warnCost: 8, maxCost: 10 });
		expect(evaluateCostGate(gate, 10)).toBe("cap");
		expect(evaluateCostGate(gate, 10)).toBe("cap");
	});
});

describe("applyCostGate", () => {
	it("dispatches below thresholds", () => {
		const gate = createCostGateController({ maxCost: 10 });
		const dispatch = () => "sent";
		expect(applyCostGate(gate, () => 1, () => {}, dispatch)).toBe("sent");
	});

	it("throws CostCapExceededError at maxCost without dispatching", () => {
		const gate = createCostGateController({ maxCost: 10 });
		const dispatched = { called: false };
		expect(() =>
			applyCostGate(gate, () => 10, () => {}, () => {
				dispatched.called = true;
			}),
		).toThrow(CostCapExceededError);
		expect(dispatched.called).toBe(false);
	});

	it("invokes onWarn once at warnCost", () => {
		const gate = createCostGateController({ warnCost: 8 });
		const warned: string[] = [];
		applyCostGate(gate, () => 8, m => warned.push(m), () => "sent");
		applyCostGate(gate, () => 9, m => warned.push(m), () => "sent");
		expect(warned).toHaveLength(1);
		expect(warned[0]).toContain("$8.00");
	});

	it("binds the cost getter on first use", () => {
		const gate = createCostGateController({ maxCost: 10 });
		applyCostGate(gate, () => 3, () => {}, () => {});
		expect(gate.getCost?.()).toBe(3);
	});
});

describe("resolveCostGate", () => {
	it("returns undefined when neither flags nor config are set", () => {
		expect(resolveCostGate({}, {})).toBeUndefined();
	});

	it("prefers flags over configured values", () => {
		const gate = resolveCostGate({ warnCost: 8, maxCost: 10 }, { warnCost: 1, maxCost: 2 });
		expect(gate?.warnCost).toBe(8);
		expect(gate?.maxCost).toBe(10);
	});

	it("falls back to configured values", () => {
		const gate = resolveCostGate({}, { warnCost: 8, maxCost: 10 });
		expect(gate?.warnCost).toBe(8);
		expect(gate?.maxCost).toBe(10);
		expect(gate?.warned).toBe(false);
	});
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: FAIL —— 模块不存在(动态 import 解析失败)

- [ ] **Step 3: 实现 cost-gate.ts**

```ts
export interface CostGateController {
	warnCost?: number;
	maxCost?: number;
	/** One-time warn flag, shared across the whole session tree. */
	warned: boolean;
	/** Bound by the root session on first dispatch. */
	getCost?: () => number;
}

export class CostCapExceededError extends Error {
	constructor(public readonly cap: number) {
		super(
			`Cost cap reached ($${cap.toFixed(2)}). Raise session.maxCost or remove --max-cost to continue.`,
		);
		this.name = "CostCapExceededError";
	}
}

export function createCostGateController(options: {
	warnCost?: number;
	maxCost?: number;
}): CostGateController {
	return { warnCost: options.warnCost, maxCost: options.maxCost, warned: false };
}

export type CostGateDecision = "ok" | "warn" | "cap";

export function evaluateCostGate(controller: CostGateController, cost: number): CostGateDecision {
	if (controller.maxCost !== undefined && cost >= controller.maxCost) return "cap";
	if (controller.warnCost !== undefined && cost >= controller.warnCost && !controller.warned) {
		controller.warned = true;
		return "warn";
	}
	return "ok";
}

export function applyCostGate(
	gate: CostGateController,
	getCost: () => number,
	onWarn: (message: string) => void,
	dispatch: () => unknown,
): unknown {
	gate.getCost ??= getCost;
	const cost = gate.getCost();
	const decision = evaluateCostGate(gate, cost);
	if (decision === "cap") {
		throw new CostCapExceededError(gate.maxCost!);
	}
	if (decision === "warn") {
		onWarn(
			`Cost warning: cumulative session cost $${cost.toFixed(2)} reached the warn threshold ($${gate.warnCost!.toFixed(2)}).`,
		);
	}
	return dispatch();
}

export function resolveCostGate(
	flags: { warnCost?: number; maxCost?: number },
	configured: { warnCost?: number; maxCost?: number },
): CostGateController | undefined {
	const warnCost = flags.warnCost ?? configured.warnCost;
	const maxCost = flags.maxCost ?? configured.maxCost;
	if (warnCost === undefined && maxCost === undefined) return undefined;
	return createCostGateController({ warnCost, maxCost });
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 全部通过(11 pass)

- [ ] **Step 5: 提交**

```bash
git add packages/coding-agent/src/session/cost-gate.ts packages/coding-agent/test/cost-gate.test.ts
git commit -m "feat(session): add cost gate controller and decision logic"
```

---

### Task 2: settings-schema 新增 session.warnCost / session.maxCost

**Files:**
- Modify: `packages/coding-agent/src/config/settings-schema.ts`(新增键 + `SessionSettings` 接口 + GroupTypeMap 的 `session` 条目)
- Test: `packages/coding-agent/test/cost-gate.test.ts`(追加用例)

**Interfaces:**
- Produces: `Settings.get("session.warnCost"): number | undefined`、`Settings.get("session.maxCost"): number | undefined`

- [ ] **Step 1: 写失败测试**

在 `test/cost-gate.test.ts` 追加:

```ts
import { Settings } from "@oh-my-pi/pi-coding-agent/config/settings";

describe("session cost settings (issue #7802)", () => {
	it("defaults warnCost and maxCost to undefined", () => {
		const settings = Settings.isolated();
		expect(settings.get("session.warnCost")).toBeUndefined();
		expect(settings.get("session.maxCost")).toBeUndefined();
	});

	it("honors explicit overrides", () => {
		const settings = Settings.isolated({ "session.warnCost": 8, "session.maxCost": 10 });
		expect(settings.get("session.warnCost")).toBe(8);
		expect(settings.get("session.maxCost")).toBe(10);
	});
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 类型错误 —— `"session.warnCost"` 不在 SettingPath 中

- [ ] **Step 3: 实现**

在 settings-schema.ts 的 `"commit.analyzeFilesMaxFiles"` 键后追加:

```ts
	"session.warnCost": { type: "number", default: undefined },

	"session.maxCost": { type: "number", default: undefined },
```

新增接口(放在 `CommitSettings` 附近):

```ts
export interface SessionSettings {
	warnCost?: number;
	maxCost?: number;
}
```

在 `GroupTypeMap` 接口(约 L5830)的 `commit: CommitSettings;` 后追加:

```ts
	session: SessionSettings;
```

- [ ] **Step 4: 运行确认通过**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 全部通过(13 pass)

- [ ] **Step 5: 提交**

```bash
git add packages/coding-agent/src/config/settings-schema.ts packages/coding-agent/test/cost-gate.test.ts
git commit -m "feat(config): add session.warnCost and session.maxCost settings"
```

---

### Task 3: CLI flag 解析 --warn-cost / --max-cost

**Files:**
- Modify: `packages/coding-agent/src/cli/args.ts:43`(maxTime 字段旁)
- Modify: `packages/coding-agent/src/cli/flag-tables.ts:154`(--max-time setter 旁)
- Test: `packages/coding-agent/test/cost-gate.test.ts`(追加用例)

**Interfaces:**
- Produces: `parsed.warnCost?: number`、`parsed.maxCost?: number`(无效值抛 `CliUsageError`)

- [ ] **Step 1: 写失败测试**

镜像 `test/cli-max-time-flag.test.ts` 的既有模式,在 `test/cost-gate.test.ts` 追加:

```ts
import { parseArgs } from "@oh-my-pi/pi-coding-agent/cli/args";
import { runCli } from "@oh-my-pi/pi-coding-agent/cli";

describe("--warn-cost / --max-cost parsing (issue #7802)", () => {
	it("parses valid numbers", () => {
		const parsed = parseArgs(["--warn-cost", "8", "--max-cost", "10", "--print", "hello"]);
		expect(parsed.warnCost).toBe(8);
		expect(parsed.maxCost).toBe(10);
		expect(parsed.print).toBe(true);
	});

	it("throws a visible parse error for invalid values", () => {
		for (const value of ["-1", "Infinity", "NaN", "abc"]) {
			let thrown: unknown;
			try {
				parseArgs(["--max-cost", value]);
			} catch (error) {
				thrown = error;
			}
			if (!(thrown instanceof Error)) {
				throw new Error(`--max-cost ${value} did not throw a visible parse error`);
			}
			expect(thrown.message).toContain("--max-cost");
		}
	});

	it("reports invalid values as CLI usage errors", async () => {
		const previousExitCode = process.exitCode;
		let observedExitCode: string | number | null | undefined;
		const captured: string[] = [];
		vi.spyOn(process.stderr, "write").mockImplementation((chunk: string | Uint8Array) => {
			captured.push(typeof chunk === "string" ? chunk : new TextDecoder().decode(chunk));
			return true;
		});

		try {
			await runCli(["--warn-cost", "abc", "--print", "hello"]);
			observedExitCode = process.exitCode;
		} finally {
			vi.restoreAllMocks();
			process.exitCode = previousExitCode ?? 0;
		}

		const stderr = captured.join("");
		expect(observedExitCode).toBe(2);
		expect(stderr).toContain("Invalid --warn-cost value");
		expect(stderr).toContain("Run `omp --help` for available flags.");
	});
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 新 3 用例失败(warnCost/maxCost 不存在)

- [ ] **Step 3: 实现**

`args.ts:43` 的 `maxTime?: number;` 后追加:

```ts
	warnCost?: number;
	maxCost?: number;
```

`flag-tables.ts` 的 `"--max-time"` setter 后追加(复用同文件的 `CliUsageError` 导入):

```ts
	"--warn-cost": (result, value) => {
		const parsed = Number(value);
		if (!Number.isFinite(parsed) || parsed < 0) {
			throw new CliUsageError(
				`Invalid --warn-cost value: ${JSON.stringify(value)}. Expected a non-negative number of USD.`,
			);
		}
		result.warnCost = parsed;
	},
	"--max-cost": (result, value) => {
		const parsed = Number(value);
		if (!Number.isFinite(parsed) || parsed < 0) {
			throw new CliUsageError(
				`Invalid --max-cost value: ${JSON.stringify(value)}. Expected a non-negative number of USD.`,
			);
		}
		result.maxCost = parsed;
	},
```

- [ ] **Step 4: 运行确认通过**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 全部通过(16 pass)

- [ ] **Step 5: 提交**

```bash
git add packages/coding-agent/src/cli/args.ts packages/coding-agent/src/cli/flag-tables.ts packages/coding-agent/test/cost-gate.test.ts
git commit -m "feat(cli): add --warn-cost and --max-cost flags"
```

---

### Task 4: main.ts 构建 costGate 控制器

**Files:**
- Modify: `packages/coding-agent/src/main.ts`(deadline 翻译处约 L908 旁)
- Test: `packages/coding-agent/test/cost-gate.test.ts`(追加接线用例,镜像 cli-max-time-flag.test.ts 的 runRootCommand DI 模式)

**Interfaces:**
- Consumes: `resolveCostGate`(Task 1)、`settings.get("session.warnCost"/"session.maxCost")`(Task 2)、`parsed.warnCost/maxCost`(Task 3)
- Produces: `options.costGate?: CostGateController`

- [ ] **Step 1: 写失败测试**

在 `test/cost-gate.test.ts` 追加(镜像 `cli-max-time-flag.test.ts` 的 "converts maxTime to an absolute session deadline" 用例):

```ts
import { AuthStorage } from "@oh-my-pi/pi-coding-agent/session/auth-storage";
import { runRootCommand } from "@oh-my-pi/pi-coding-agent/main";
import { TempDir } from "@oh-my-pi/pi-utils";
import type { CreateAgentSessionOptions } from "@oh-my-pi/pi-coding-agent/sdk";
import * as path from "node:path";

describe("cost gate wiring in runRootCommand (issue #7802)", () => {
	it("passes flag-derived costGate into createAgentSession options", async () => {
		using tempDir = TempDir.createSync("@omp-cost-gate-");
		const authStorage = await AuthStorage.create(path.join(tempDir.path(), "auth.db"));
		const settings = Settings.isolated({ "marketplace.autoUpdate": "off" });
		let observedOptions: CreateAgentSessionOptions | undefined;
		const parsed = parseArgs(["--warn-cost", "8", "--max-cost", "10", "--print", "hello"]);
		parsed.noExtensions = true;
		parsed.noSkills = true;
		parsed.noRules = true;
		parsed.noTools = true;
		parsed.noLsp = true;
		parsed.sessionDir = tempDir.path();

		try {
			await runRootCommand(parsed, ["--warn-cost", "8", "--max-cost", "10", "--print", "hello"], {
				discoverAuthStorage: async () => authStorage,
				settings,
				createAgentSession: async options => {
					observedOptions = options;
					throw new Error("stop after session options");
				},
			});
		} catch (error) {
			if (!(error instanceof Error) || error.message !== "stop after session options") {
				throw error;
			}
		} finally {
			authStorage.close();
		}

		expect(observedOptions?.costGate?.warnCost).toBe(8);
		expect(observedOptions?.costGate?.maxCost).toBe(10);
	});

	it("falls back to session settings when flags are absent", async () => {
		using tempDir = TempDir.createSync("@omp-cost-gate-");
		const authStorage = await AuthStorage.create(path.join(tempDir.path(), "auth.db"));
		const settings = Settings.isolated({
			"marketplace.autoUpdate": "off",
			"session.warnCost": 8,
			"session.maxCost": 10,
		});
		let observedOptions: CreateAgentSessionOptions | undefined;
		const parsed = parseArgs(["--print", "hello"]);
		parsed.noExtensions = true;
		parsed.noSkills = true;
		parsed.noRules = true;
		parsed.noTools = true;
		parsed.noLsp = true;
		parsed.sessionDir = tempDir.path();

		try {
			await runRootCommand(parsed, ["--print", "hello"], {
				discoverAuthStorage: async () => authStorage,
				settings,
				createAgentSession: async options => {
					observedOptions = options;
					throw new Error("stop after session options");
				},
			});
		} catch (error) {
			if (!(error instanceof Error) || error.message !== "stop after session options") {
				throw error;
			}
		} finally {
			authStorage.close();
		}

		expect(observedOptions?.costGate?.warnCost).toBe(8);
		expect(observedOptions?.costGate?.maxCost).toBe(10);
	});
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 新 2 用例失败(`observedOptions.costGate` 为 undefined)

- [ ] **Step 3: 实现**

在 `main.ts` 的 `if (parsed.maxTime !== undefined) { options.deadline = ... }`(约 L908)后追加:

```ts
	const costGate = resolveCostGate(
		{ warnCost: parsed.warnCost, maxCost: parsed.maxCost },
		{
			warnCost: activeSettings.get("session.warnCost"),
			maxCost: activeSettings.get("session.maxCost"),
		},
	);
	if (costGate !== undefined) {
		options.costGate = costGate;
	}
```

在 main.ts 顶部 import 区追加:

```ts
import { resolveCostGate } from "./session/cost-gate";
```

- [ ] **Step 4: 运行确认通过**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 新 2 用例通过(此时 Task 5 的 CreateAgentSessionOptions.costGate 尚未定义,类型检查会报错——Task 5 Step 3 定义后复查)

- [ ] **Step 5: 提交**

```bash
git add packages/coding-agent/src/main.ts
git commit -m "feat(cli): wire cost gate from flags and settings into session options"
```

---

### Task 5: sdk.ts streamFn 分发前门禁

**Files:**
- Modify: `packages/coding-agent/src/sdk.ts`(CreateAgentSessionOptions 约 L405 附近;streamFn 包装器 L3206)
- Test: `packages/coding-agent/test/cost-gate.test.ts`(追加)

**Interfaces:**
- Consumes: `applyCostGate`、`CostGateController`(Task 1)
- Produces: `CreateAgentSessionOptions.costGate?: CostGateController`

- [ ] **Step 1: 写失败测试(applyCostGate 经 streamFn 接线)**

gate 判定逻辑已在 Task 1 单测;此处验证 `applyCostGate` 的流式接线契约(dispatch 未执行、onWarn 收到消息)——用假 dispatch/假 onWarn,确保 sdk 的接线方式与 Task 1 的契约一致:

```ts
describe("streamFn cost gate contract (issue #7802)", () => {
	it("warns through onWarn and still dispatches below maxCost", () => {
		const gate = createCostGateController({ warnCost: 8, maxCost: 10 });
		const messages: string[] = [];
		const dispatched: unknown[] = [];
		applyCostGate(gate, () => 8, m => messages.push(m), () => dispatched.push("sent"));
		expect(messages).toHaveLength(1);
		expect(dispatched).toEqual(["sent"]);
	});

	it("never dispatches once maxCost is reached", () => {
		const gate = createCostGateController({ maxCost: 10 });
		const dispatched: unknown[] = [];
		expect(() => applyCostGate(gate, () => 10, () => {}, () => dispatched.push("sent"))).toThrow(
			CostCapExceededError,
		);
		expect(dispatched).toHaveLength(0);
	});
});
```

- [ ] **Step 2: 运行确认通过**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts`
Expected: 通过(契约用例成立;sdk.ts 的接线在 Step 3 后由 check:types + Task 6 集成验证兜底)

- [ ] **Step 3: 实现**

`CreateAgentSessionOptions` 接口(约 L405 `providerSessionId?: string;` 附近)追加:

```ts
	/** Cost gate controller shared across the session tree; checked before each provider dispatch. */
	costGate?: CostGateController;
```

sdk.ts 顶部 import 区追加:

```ts
import { applyCostGate, type CostGateController } from "./session/cost-gate";
```

streamFn 包装器(L3206)改为:

```ts
			streamFn: (streamModel, context, streamOptions) => {
				if (notifyFirstChatDispatch) {
					const cb = notifyFirstChatDispatch;
					notifyFirstChatDispatch = undefined;
					try {
						cb();
					} catch (err) {
						logger.warn("onFirstChatDispatch hook threw", {
							error: err instanceof Error ? err.message : String(err),
						});
					}
				}
				const gate = options.costGate;
				if (gate === undefined) {
					return settingsAwareStreamFn(streamModel, context, streamOptions);
				}
				// First dispatcher (the root session) binds the authoritative
				// cumulative cost getter; subagent sessions inherit it.
				gate.getCost ??= () => session.getSessionStats().cost;
				return applyCostGate(
					gate,
					() => gate.getCost!(),
					message => session.emitNotice("warning", message, "cost-gate"),
					() => settingsAwareStreamFn(streamModel, context, streamOptions),
				);
			},
```

(`session` 是 `let session!: AgentSession`(L1585),闭包执行时已赋值。)

- [ ] **Step 4: 类型检查**

Run: `cd packages/coding-agent && bun run check:types`
Expected: 无错误(此时 Task 4 的 main.ts 接线也通过)

- [ ] **Step 5: 提交**

```bash
git add packages/coding-agent/src/sdk.ts packages/coding-agent/test/cost-gate.test.ts
git commit -m "feat(sdk): enforce cost gate before every provider dispatch"
```

---

### Task 6: 子代理会话透传 costGate + 集成验证

**Files:**
- Modify: `packages/coding-agent/src/task/executor.ts`(buildSubagentSessionOptions 约 L2999-3060)
- Test: 集成验证(print 模式实测,见 Step 3)

**Interfaces:**
- Consumes: `options.costGate`(Task 5 的 CreateAgentSessionOptions)

- [ ] **Step 1: 实现**

`buildSubagentSessionOptions` 返回对象(约 L3002 起)追加:

```ts
				costGate: options.costGate,
```

- [ ] **Step 2: 类型检查 + 现有 task 测试回归**

Run: `cd packages/coding-agent && bun run check:types && bun test test/rpc-subagents.test.ts test/agent-session-eager-task.test.ts`
Expected: 无类型错误,测试通过

- [ ] **Step 3: 集成验证(真实会话 + 真实模型,可选——需要 DEEPSEEK_API_KEY)**

用工作区已有驱动脚本扩展:复制 `scripts/repro-7833-e2e-tool.ts` 为 `scripts/verify-7802-cost-gate.ts`,在 `createAgentSession` options 中传 `costGate: createCostGateController({ maxCost: 0.0001 })`(极低阈值),对 /tmp/omp-repro-7833-big 跑一次工具执行,预期第一次分发即抛 `CostCapExceededError`;再把 maxCost 设为 0.00001(低于一次分发的成本),确认 warn 路径输出 notice。

Run: `DEEPSEEK_API_KEY=... bun scripts/verify-7802-cost-gate.ts`
Expected: 脚本打印 `CostCapExceededError` 或 notice 输出,与阈值设置对应

- [ ] **Step 4: 验证 print 模式触顶行为并补齐 exit code(如需要)**

镜像 `cli-max-time-flag.test.ts` 的 runCli 模式写一次性验证:在 `--print` 模式下跑到门禁触发,观察进程退出码与输出。

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts` 内新增用例(或临时脚本):
用 `runCli(["--print", "hello"])` + 注入 `createAgentSession` 返回一个会在首次分发抛 `CostCapExceededError` 的假会话,断言输出包含 "Cost cap reached" 且 `process.exitCode` 为 0。

若实测退出码非 0,在 `main.ts` 的 print 错误处理路径把 `CostCapExceededError` 映射为 exit 0(镜像 deadline 超时的处理),并补一个断言用例。

- [ ] **Step 5: 提交**

```bash
git add packages/coding-agent/src/task/executor.ts
git commit -m "feat(task): thread the session cost gate into subagent sessions"
```

---

### Task 7: 回归验证

**Files:** 无修改

- [ ] **Step 1: 跑相关回归测试**

Run: `cd packages/coding-agent && bun test test/cost-gate.test.ts test/commit-analyze-files-cap.test.ts test/issue-7833-analyze-files-fanout.test.ts test/commit-execute.test.ts test/commit-agentic-attribution.test.ts`
Expected: 全部通过

- [ ] **Step 2: 类型检查**

Run: `cd packages/coding-agent && bun run check:types`
Expected: 无错误

- [ ] **Step 3: 提交(如有残留)**

```bash
git status --short
git commit -am "chore(session): verify cost gate regression" # 仅在确有改动时
```
